name: Build memory v0.7 APK PR
on:
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Apply v0.5 copy fixes
        run: python3 mnemonic-bridge-v0.4/patch_v05.py
      - name: Apply v0.6 stability fixes
        run: python3 mnemonic-bridge-v0.4/patch_v06.py
      - name: Apply memory v0.7 persistence fixes
        run: python3 mnemonic-bridge-v0.4/patch_v07.py
      - name: Fix memory v0.7 Build import
        run: python3 mnemonic-bridge-v0.4/patch_v07_fix.py
      - name: Build APK
        working-directory: mnemonic-bridge-v0.4
        run: gradle :app:assembleDebug --stacktrace
      - name: Rename APK
        run: cp mnemonic-bridge-v0.4/app/build/outputs/apk/debug/app-debug.apk memory-v0.7.apk
      - uses: actions/upload-artifact@v4
        with:
          name: memory-v0.7
          path: memory-v0.7.apk
          if-no-files-found: error
